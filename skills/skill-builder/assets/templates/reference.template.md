# <Topic>

<One line on what this reference covers and when the body should load it.>

## <Section>

<Detailed rules, catalogs, or examples that don't belong in the SKILL.md body. This file is loaded on demand,
so depth is fine here — but keep it focused on a single topic so each load stays cheap.>

<example>
<Concrete example, wrapped in example tags so the model distinguishes it from instructions.>
</example>
